version: '3.8'

services:
  flutter-frontend:
    build:
      context: .
      dockerfile: Dockerfile
      target: runtime
    ports:
      - "3000:80"
    restart: unless-stopped
    environment:
      - NODE_ENV=production
